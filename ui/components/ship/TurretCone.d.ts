import { FC } from "react";
import { GameConstants, Ship as ShipType, TurretStation } from "../../types";
export declare const TurretCone: FC<{
    turret: TurretStation;
    ship: ShipType;
    constants: GameConstants;
}>;
