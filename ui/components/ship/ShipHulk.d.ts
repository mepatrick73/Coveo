import { FC } from "react";
import { GameConstants, Ship as ShipType } from "../../types";
export declare const ShipHulk: FC<{
    ship: ShipType;
    constants: GameConstants;
    showGrid: boolean;
}>;
